## Checkpoint: RAG retrieval update

### Completed
- Inspected how retrieval selects chunks and where `k` is set
- Added an adaptive retrieval strategy based on repo size
- Ensured at least one chunk per file when feasible
- Added a cap to prevent prompt overload in large repos
- Balanced coverage vs. relevance with a hybrid retrieval approach
- Added retrieval limit configuration with defaults
- Added basic sanity tests for retrieval behavior

### Verified
- Retrieval tests pass
- Config defaults still work when env vars are not set

### To validate after deployment
- Confirm citations still appear correctly
- Check chunk diversity on small, medium, and large repos
- Verify prompt size stays within budget
- Compare output quality before and after the retrieval changes

### Follow-up items
- Remove any remaining debug prints
- Add temporary logging for retrieval count and chunk coverage if needed
- Consider a final prompt summary showing how many files were represented
- Consider batching if full-repo coverage exceeds context limits